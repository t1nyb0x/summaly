/**
 * To absolute and sanitize URL
 * @param url URL (absolute or relative)
 * @param base Base URL
 * @returns absolute URL
 */
export declare function cleanupUrl(url: string | null, base: string): string | null;
